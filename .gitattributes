# Treat template files as text
*.tmpl text

# Ensure shell scripts have LF line endings
*.sh text eol=lf

# Ensure .git directories in flavors/themes are tracked
dot_config/yazi/flavors/**/.git/** -diff -merge
